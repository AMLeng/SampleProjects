class IntArray{
    int[] nums;
    void make_array(int size, int value){
        nums = NewArray(size, int);
        for(int i=0;i<size;i++){
            nums[i] = value;
        }
    }
    int get(int index){
        return nums[index];
    }
}

int main(){
    int[] nums;
    IntArray thing;

    thing = new IntArray;
    thing.make_array(5,1);
    nums = NewArray(5,int);
    nums[0] = 1;
    nums[1] = 1;
    for(int i=2; i<5; i=i+1){
        nums[i] = nums[i-2] + nums[i-1];
    }
    for(int i=0; i<5; i = i+1){
        Print(nums[i] - thing.get(i));
    }
}
